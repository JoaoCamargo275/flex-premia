# URL do backend (Express). Em dev local, o padrão já funciona.
# Em produção, aponte para a URL pública do backend no Render, ex:
# VITE_API_URL=https://flexpremia-backend.onrender.com
VITE_API_URL=http://localhost:4000

# Definido apenas no ambiente de build do deploy (GitHub Actions), não aqui:
# BASE_PATH=/nome-do-repo/
